# Phase 1 architecture configuration baseline.
# Values are explicit and externalized to satisfy config-driven design.

paths:
  repository_root: .
  dataset_dir: India_runs_data_and_ai_challenge
  candidates_path: India_runs_data_and_ai_challenge/candidates.jsonl
  job_description_path: India_runs_data_and_ai_challenge/job_description.docx
  submission_output_path: outputs/submission.csv

runtime:
  top_n: 100
  random_seed: 42
  strict_validation: true
  max_candidates_to_process: null

retrieval:
  strategy: hybrid
  initial_k: 2000

ranking:
  strategy: evidence_weighted
  confidence_threshold: 0.5

evaluation:
  top_k_values: [10, 20, 50]
  enable_ablation: true
  enable_robustness: true
  experiment_log_path: experiments/experiment_log.jsonl

logging:
  level: INFO

dashboard:
  default_page_size: 25
  max_comparison_candidates: 4
  enable_engineering_view: true
  shortlist_output_path: outputs/shortlist.csv
